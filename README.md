# itruns-consult

> An interactive consultation with Dokbot

## Setup

``` bash
# install yarn
npm install -g yarn

# install dependencies
yarn install

# serve with hot reload at localhost:8080
yarn run dev

# build for production with minification
yarn run build
```
